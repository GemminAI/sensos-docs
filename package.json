{
  "name": "sensos-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SensOS Developer Portal — official documentation SSOT",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "audit:links": "node scripts/audit-links.mjs",
    "release:check": "npm run build && npm run audit:links"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.12.0",
    "sharp": "^0.34.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
